<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.justinlegrand.bookreviewlog">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.BookListActivity"
            android:label="@string/title_activity_book_list" />
        <activity
            android:name=".ui.BookReview"
            android:label="@string/title_activity_book_review" />
        <activity
            android:name=".ui.Settings"
            android:label="@string/title_activity_settings" />

        <!-- Added Search activity to use android search function-->
        <!--DISABLING TO DEBUG
        <activity android:name=".model.Search" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>

        <!-End Search Activity-->

    </application>
</manifest>
